apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: webproject
spec:
  selector:
    app: frontend
    # Blue/Green routing: Change this selector between 'blue' and 'green' to switch traffic
    version: blue 
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
